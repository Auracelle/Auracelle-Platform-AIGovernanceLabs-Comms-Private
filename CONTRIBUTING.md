# Contributing (Internal)

This repository is a self-contained HTML artifact.

Recommended workflow:
1. Create a branch.
2. Make changes to `index.html`.
3. Open a PR with screenshots and a short summary.
4. Merge to `main` to deploy via GitHub Pages.
